# 安全策略

## 隐私立场(本项目最重要的一节)

- **本地优先**:引擎只读写你显式选定的 vault 目录,不出机器
- **零遥测**:本仓库代码不收集、不上报任何数据
- **检索零外发**:检索前缀固定本地合成(`--no-llm`);BM25 索引为本地派生态
- **联网均显式授权**:URL 抓取、Ollama 重排、上游 Defuddle 等可选工具,技能规程要求先征得用户同意才触网,且默认只指向 localhost 或用户提供的地址
- **git 存档仅本地**:checkpoint 只做本地提交,本仓库工具绝不 push 你的 vault

## 报告漏洞

请通过 GitHub 的 **私有漏洞报告**(Private Vulnerability Reporting / Security Advisories)提交,不要开公开 issue。

- 修复窗口:尽力在 7 天内响应
- 支持版本:最新 main 分支

## 你需要注意的

- 工具会写你的文件(经事务与哈希校验保护,但仍是写操作)——重要 vault 务必备份
- `.raw/captured/` 中的素材副本会长期保留(溯源设计);不想留痕的材料不要摄入
